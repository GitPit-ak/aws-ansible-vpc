- name: Setup both web servers
  hosts: all
  become: yes
  tasks:
  - name: Insert/Update "Match User" configuration block in /etc/ssh/sshd_config
    ansible.builtin.copy:
      content: "cat /home/ubuntu/aws-ansible-vpc/vars/output_vars"
      dest: /home/ubuntu/aws-ansible-vpc/vars/vpc_vars